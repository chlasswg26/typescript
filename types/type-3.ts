export interface GreetingByTimeType {
    (time: string): string
}

export interface HitungPanjangStringType {
    (input: string): string
}

export interface PembulatanAngkaType {
    (angka: number): string
}
